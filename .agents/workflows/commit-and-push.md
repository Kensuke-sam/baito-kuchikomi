---
description: 変更内容を確認して問題なければpushする
---

1. `git status` を実行して現在の状態を確認してください。
// turbo
2. `git show` を実行して、直近のコミットの差分（diff）を確認してください。
3. 意図しない変更が含まれていないかチェックし、問題がないかユーザーに確認してください。**確認の際は必ず日本語で「変更内容に問題はないでしょうか？」と聞いてください。**
4. ユーザーから問題ない旨の返答が得られたら、`git push` を実行してください。
